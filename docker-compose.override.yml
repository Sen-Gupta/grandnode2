version: '3.4'

services:
  grand.web:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:80
    ports:
      - "5010:80"      
    volumes:
      - ${APPDATA}/ASP.NET/Https:/root/.aspnet/https:ro